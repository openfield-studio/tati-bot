# tati-bot イベント駆動戦略・レジームフィルター深掘り手順書

2026-09-24、1306/市場全体イベントに対するロング/ベアの検証(SESSION_LOG項目81〜95)を受けて
作成。この日1日で「利確・損切り・反応速度・連続損切りレジーム・移動平均レジーム」と多くの
アイデアを試し、2回(連続損切り閾値2%、移動平均MA200)過学習の罠(パラメータを少し変えると
結果が大きく崩れる)にハマった反省を踏まえた手順書。

**クラウドルーティンでは実行できない**(yfinance/^N225ともクラウド実行環境からは接続不可、
既存の日次自動探索ループのルール8と同じ制約)。ユーザーがローカルセッションで「続き」
「検証して」等を明示的に依頼した時のみ、このファイルの手順に従う。

## 対象ファイル

- `market_events.json`/`market_event_bear_lab.py`: 1306全体、手動キュレーションのイベント(項目81)
- `company_events.json`/`company_event_study_lab.py`: 個別企業、手動キュレーションのイベント(項目82)
- `systematic_decline_short_lab.py`: 1306の機械検出ショック日(n=53、2009〜2026年、選択バイアスなし、項目90)
- `nikkei_long_term_stoploss_lab.py`: 日経平均(^N225)の機械検出ショック日(n=169、1965〜2026年、
  選択バイアスなし・本物の下落相場を含む、項目93〜95)
- FACTOR_RESEARCH_LOG.mdの調査待ちキュー#16「トレンドフィルター付き押し目買い」がこの
  研究テーマの本体。ここでの進捗は#16に追記していく。

## 手順

1. 作業開始前に`git pull origin main`。SESSION_LOG.mdの直近項目(81〜)とFACTOR_RESEARCH_LOG.md
   のキュー#16を読んで前回までの進捗を把握する。
2. **1回のセッションで新しいアイデアは1つだけ試す**(既存の日次探索ルール1と同じ精神)。
   例: 「ショックの規模を重み付けに使う」「反発の速さ自体を判定材料にする」「複数指標の
   組み合わせ(MA+ボラティリティ等)」等。
3. **新しいアイデアは必ず`systematic_decline_short_lab.py`(n=53)と
   `nikkei_long_term_stoploss_lab.py`(n=169、本物の下落相場を含む)の両方で検証する**。
   片方だけの結果では判断しない(2009年以降だけだと好成績に見えて全期間だと崩れた
   項目93の教訓)。
4. **パラメータ(閾値・窓幅等)を振る場合は、必ず最良値の隣接値も確認する**
   (2026-09-24追加、最重要ルール)。例えば閾値2%が良く見えても1.5%と2.5%も試し、
   なめらかに変化しているか確認する。隣接値で結果が大きく崩れる(ジグザグする)場合は
   過学習と判定し、「不採用」としてキュー#16に理由を記録して終了する。良い数字が
   1点だけ孤立して出ている場合は疑うこと。
5. 検知した「下落局面」「有利/不利な判定」の**年度分布を必ず確認する**(特定の1〜2年に
   偏っていないか)。n(検出件数)が1桁前半など極端に少ない場合は「判断不能」として
   結論を出さない。
6. 結果は勝率・平均・**中央値**・合計損益をセットで報告する(合計だけで判断すると
   外れ値1件に支配される、項目87で繰り返し確認した罠)。
7. データが取得できない・不完全な場合は結果を捏造しない。理由をSESSION_LOG.mdに
   1行記録して終了する。
8. 結果はSESSION_LOG.mdに通し番号で追記し、FACTOR_RESEARCH_LOG.mdのキュー#16の
   概要・実証状況欄も更新する。
9. **5回試して(過学習で不採用になったものも含めて数える)芽が出なければ、この設計
   アプローチ(離散的なショック検知+二値レジーム判定)自体を疑い、方針を変える**
   (例: 連続値のシグナルとして重み付けする、複数指標のスコア合成にする、そもそも
   ショック単位でなく通常の日次モデルに戻す等)。ユーザーに一度立ち止まって相談する。
10. 有望な設計が見つかっても、**本体(value_screener.py・trading_agents.py)への組み込みは
    ユーザーに確認してから**(自動では行わない、既存ルール6と同じ)。
