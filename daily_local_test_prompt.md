# tati-bot 日次ローカル実証テスト・手順書

**2026-09-17追記**: 当初はWindowsタスクスケジューラから`claude -p`(非対話モード)に
このまま渡して無人実行させる想定だったが、Claude Code自体の安全機構により
「無人でclaudeを起動するスクリプトの作成」がブロックされ、この方式は断念した
(SESSION_LOG.md項目49参照)。実際の自動化は`check_queue_and_notify.ps1`
(候補ありをWindows通知するだけ、Claudeは起動しない)に落ち着いている。

このファイルは無人実行では使われなくなったが、ユーザーから「テストして」と
依頼された際にローカルセッション(人間相手の対話セッション)が従うべき手順・
ルールのチェックリストとして引き続き有効。

---

tati-bot日次ローカル実証テスト担当。クラウドの「tati-bot週次ファクター探索」ルーティン
(毎日21:00 JST)が文献調査で見つけた候補を、実際のyfinanceデータが使えるこのローカルPC上で
検証するのが役割。日本語で応答。

作業開始前に必ず `git pull origin main` を実行し、クラウドルーティンが追加した最新の
キュー内容を取り込むこと。

手順:
1. SESSION_LOG.mdとFACTOR_RESEARCH_LOG.md(リーダーボード・調査待ちキュー・本体組み込みの
   昇格基準を含む全体)を読む。
2. 調査待ちキューに「未実証(文献調査のみ)」の候補が1件もなければ、何もせずに終了する
   (通知不要。無理に何かを検証しない)。
3. 候補が1件以上あれば、**最も古い(発見日が早い)ものを1件だけ**選んで検証する
   (複数同時に手を広げない)。
   - individual_value_backtest.py / new_factors_lab.py と同じ先読みバイアス対策
     (決算期末+60日後の株価を基準、FORWARD_MONTHS=[6, 12])・年度内クロスセクショナル
     比較(下位1/3 vs 上位1/3)の型で新しいラボスクリプト(`<factor>_lab.py`)を書く。
     一覧比較や逆張り系などファクターの性質がこの型に合わない場合は、既存の
     `trader_methods_lab.py` や `seasonal_effects_lab.py` 等、性質が近いスクリプトの
     型を流用してよい。
   - **必ず `tse_prime_universe.csv`(東証プライム全銘柄、約1,556銘柄)をユニバースに
     使う**(日経225のみでは効果が実際より弱く見えることが判明済み)。
   - 重い処理には必ず25銘柄ごとのチェックポイント(JSON、完了時に別キャッシュへ)を
     実装する(チェックポイントなしで走らせて1,250/1,556でクラッシュし進捗を全損した
     実例あり。必ず最初から入れること)。
   - 集計時は `RET_CAP = 2.0`(|リターン|>200%は異常値として除外)を必ず適用する
     (yfinanceのデータ不具合で1銘柄の異常値が集計全体を歪めた実例あり)。
   - データが取得できない・不完全な場合は結果を絶対に捏造しない。その場合は
     キューのエントリはそのまま残し、SESSION_LOG.mdに理由を1行記録して終了する。
   - **判定は独立な単位で数えた統計で行う(2026-09-19追加、FACTOR_RESEARCH_LOG.md
     「横断的考察」1節)**。同じ年度・同じ日の銘柄は連動するので、プールした観測数の
     t値は大幅に過大になる。
     - 財務系(年度内クロスセクション型): 年度ごとの3分位スプレッドを出し、「何年度中
       何年度でプラスか」と年度間t値(平均÷標準誤差、年度を1観測)を主な判定材料にする
       (`factor_synthesis_lab.py`のsection_a/fm_regressionを流用可)。
     - イベント系(特定日の値動き): 日付ごとの平均リターンから同日の1306(TOPIX)の
       値動きを引いた超過リターンを、日付を1観測として検定する。
     - プールしたt値・Bonferroni補正は参考値として併記してよいが、それだけでGOにしない。
   - **既存のバリュー指標(B/M)との同時投入でも残るか**を必ず確認する。B/Mと相関する
     指標は、この期間はB/Mの効果の言い換え(または裏返し)に見えやすい。
4. 初回の単純な3分位比較で明確にプラス(GO)の結果が出た場合は、そこで結論を出さず
   **多角的検証**を追加で行う(既存の `new_factors_angles.py` / `new_factors_crash_lab.py`
   と同じ3方向): (a) 5分位での再検証、(b) 個別銘柄の割安さ(PER)ファクターとの
   組み合わせでの再検証、(c) 実際の急落局面(`defensive_stock_lab.py`の`CRASHES`定義:
   2024-07-11〜2024-08-05、2024-12-27〜2025-04-07)限定での再検証。
5. 結果に応じてログを更新する:
   - FACTOR_RESEARCH_LOG.mdの「試行済みファクター一覧」に新しい行を追加し、
     累積試行数を1増やす(多角的検証を行った場合も1トライアルとしてカウント。
     角度違いの再検証は同じ#の脚注に追記する形で、別トライアルとしては数えない)。
   - 調査待ちキューから該当行を削除する。
   - GOの場合はリーダーボードに追加(既存行の上書きはしない)。NO-GOの場合は
     リーダーボードに載せない(既存の運用ルールと同じ)。
   - SESSION_LOG.mdに新しい番号の項目として追記する。
   - factor_research_summary.jsonを最新化(既存スキーマ維持、cumulative_trials・
     latest_run・leaderboardを更新)。
6. FACTOR_RESEARCH_LOG.mdの「本体組み込みの昇格基準」(DSR通過相当かつ複数回・別期間で
   再現の両方)を満たすファクターがあれば「本体組み込み候補」表に追加し、その旨を
   通知に含める(実装はしない、提案のみ)。
7. 変更したファイルを `git add`(キャッシュ・チェックポイントJSON等の.gitignore対象は
   含めない)し、`git commit`(コミットメッセージ末尾に
   `Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>` を付与)、
   `git push origin main`(HEADがdetachedの場合は`git push origin HEAD:main`で
   直接pushしてよい)する。**これは2026-09-17にユーザーがこの日次ローカル自動化の
   設計について明示的に承認済みの標準動作であり、pushのたびに確認を取る必要はない**。
8. 最後に必ずPushNotificationで結果を要約して知らせる(候補が0件で何もしなかった日は
   通知不要)。例:「純株式発行アノマリーを検証: NO-GO(12ヶ月差-2.1pt、多角的検証でも
   反転)。累積試行数25。」

**絶対に守る制約(クラウドルーティンと共通)**:
- trading_agents.py・research_agents.py・value_screener.pyの実弾/スコアリングロジックは
  絶対に変更しない。どんなに有望な結果でも自動実装は絶対にしない(コード提案のみ、
  適用はユーザーの明示的なOKを待つ)。
- api.txt等の認証情報ファイルは絶対に読み込まない。
- データなしで検証結果を捏造しない。
- 1日1件のみ検証する(複数候補があっても最古の1件だけ、多重検定を無闇に増やさない)。
