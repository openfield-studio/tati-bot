# 1306/TOPIXの値動きに影響したと考えられる主要事象の年表

2026-09-24、ユーザーの依頼「過去の値動きにほぼ確実に影響したであろう事象をデータとして
残して生かしたい」を受けて作成。

## 位置づけ

- これは**説明的(descriptive)な年表**であり、既存のファクター検証(#33 BOJ会合効果、
  #34 NFP効果、#78 外国人投資家フロー、いずれもNO-GO)とは目的が違う。
  それらは「定型的なイベント日に系統的な予測力があるか」を検定したもので結果はNO-GOだったが、
  ここに載せるのは「個別の歴史的大事件が、その時その時の値動きにほぼ確実に影響した」という
  **事後的にほぼ自明な因果関係**の記録。両者は矛盾しない(定型繰り返しイベントに系統的優位性は
  無いが、個々の一回性の大事件は明らかに値動きを動かした、ということ)。
- 機械可読データは`market_events.json`(date/name/category/direction/confidence/description/source)。
  人間向けの解説・注意点はこのファイルに書く。
- **本体(value_screener.py・trading_agents.py)には未組み込み**。ルール6
  (週次自動探索ループのルール)と同じ方針で、実弾に関わるコードへの反映は
  ユーザー確認後のみ行う。

## データの限界(先に明記)

- yfinanceの1306価格データは**2009-01-05以降**しか取れない(確認済み、2026-09-24)。
  1306自体の上場は2001-07-11だが、それより前・2009年以前の事象については
  年表には載せても、1306自体の値動きとの対応付けは別のデータソース(日経平均等の
  長期指数データ)が必要。
- `confidence`フィールドの意味:
  - `web_verified_2026-09-24`: このセッションでWebSearchにより日付・数値を一次〜準一次
    情報で確認済み
  - `well_known`: 広く合意された歴史的事実として記載(このセッションでの個別確認はしていない)。
    実際の検証・売買判断に使う前には、影響を受ける事象だけ改めて確認すること(ルール9に準拠)
- 2026-03-09/06-08/09-11の3件は前回のセッション(モデルの知識カットオフ2026年1月)より新しい
  出来事で、今回のWeb検索で新たに発見・確認した。

## 既存の急落局面定義との対応

`defensive_stock_lab.py`の`CRASHES`(β検証用に事前定義済み)は、この年表の一部の
「結果(値動き)」に対応する「原因」を持つ:

| CRASHESの局面 | 対応する原因イベント(本年表) |
|---|---|
| 2024年8月急落(peak 2024-07-11 / trough 2024-08-05) | 2024-07-31 日銀追加利上げ → 2024-08-05 令和ブラックマンデー |
| 2025年4月急落(peak 2024-12-27 / trough 2025-04-07) | 2025-04-02 トランプ関税リベレーションデー → 2025-04-07 底値 |

## 使い道の案(実装はまだしていない、ロードマップ)

ユーザーの最終目標は「イベントを検知して自動売買に生かしたい」。ただし本体への実装は
過去の合意(「君が実装してもいいと思うものだけ実装してみて」→スコアロジックはユーザー承認後のみ)
に従い、段階を踏む必要がある:

1. **(今回やった)事象の年表を構造化データとして保存** — 完了
2. **バックテスト結果の解釈補助として使う** — 既存/将来のOOS検証で異常なドローダウンが
   出た期間を、この年表と自動で突き合わせて「この期間はXXが原因」と注記できるようにする
   (`factor_synthesis_lab.py`等の出力に組み込む案。ラボ側の改修のみで実弾には影響しない)
3. **イベント検知→シグナルとしての実証検証** — 「利上げ発表当日〜翌営業日に売る」
   「関税ショックのような地政学イベント後にディフェンシブ株にシフトする」等の具体的な
   売買ルールを、この年表の過去事象をサンプルとして統計的に検証する。
   **注意点**: イベント自体は稀(全期間で30件程度)なので、独立サンプル数が非常に少ない
   ([[feedback_tati_bot_multi_angle_testing]]の「年度・日付単位で数える」原則がここでも
   重要、n=30弱では検定力が乏しい点を認識した上で進める)。また「事後的に見れば明らかに
   影響した」ことと「事前に(リアルタイムで)検知して売買判断に使える」ことの間には
   ギャップがある(利上げ発表は既知の会合日程で予測可能だが、地震・地政学イベントは
   予測不可能で「検知」ではなく「発生後の反応ルール」しか作れない)。
4. **リアルタイム検知の実装(将来・要ユーザー承認)** — 3で有望と分かった場合のみ、
   ニュースAPI/カレンダーAPI等でイベント発生を検知する仕組みを検討。現時点では未着手。

## 更新方法

新しい大事件が起きたら`market_events.json`に1件追記する。日付・数値は原則WebSearchで
一次〜準一次情報を確認してから記載する(ルール9)。四半期に一度など、定期的な棚卸しの
仕組みは未定(必要になったら週次自動探索ループのルールに追加を検討)。
